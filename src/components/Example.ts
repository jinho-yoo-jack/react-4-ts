function isUserNameEmpty(str: string): boolean {
    return str.length === 0 || str == "";
}

// Balacne -> 금융 도메인에서는 "잔액" 의미로 해석 된다.
// 특정한 고객의 Account에서 잔고를 조회한다.
function fetchBalance(account: string) {
    const balance = 100;
    return balance;
}

if()